# lixitransfert-site

Distribution de LixiTransfert : `version.json` (manifeste de mise a jour lu par l'application via GitHub Pages) + paquets zip en assets de releases.

Publie par `publish-update.ps1` du depot principal (prive).